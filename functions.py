# Double the value of a number.
# Input: a number to be doubled
# Result: a number
def double(n:int) -> int:
    result = n * 2   # originally n * n, but the test is doubled, not squared.
    return result
